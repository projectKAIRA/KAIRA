---
import Base from '../layouts/Base.astro';

const tiers = [
  {
    name: 'Website Build',
    price: 'From $1,000',
    cadence: 'one-time project',
    blurb: 'A complete, custom professional website. Final pricing depends on scope — pages, features, and content.',
    features: [
      'Custom mobile-first design',
      'Up to 5 pages (more available)',
      'Contact form & Google Maps',
      'Technical SEO foundations',
      'Fast, secure hosting setup',
      'Launch support & training',
    ],
    cta: 'Get a Quote',
    featured: true,
  },
  {
    name: 'Care Plan',
    price: 'From $75/mo',
    cadence: 'monthly maintenance',
    blurb: 'Keep your site secure, updated, and fresh — we handle the technical side so you never have to.',
    features: [
      'Software updates & security',
      'Daily backups',
      'Uptime monitoring',
      'Monthly content updates',
      'Priority email support',
      'Monthly health report',
    ],
    cta: 'Ask About Care Plans',
    featured: false,
  },
  {
    name: 'SEO Boost',
    price: 'Custom',
    cadence: 'monthly engagement',
    blurb: 'Ongoing local SEO to climb search results and bring in more nearby customers over time.',
    features: [
      'Local keyword strategy',
      'Google Business Profile optimization',
      'On-page improvements',
      'Content recommendations',
      'Monthly ranking & traffic report',
      'Pairs with any Care Plan',
    ],
    cta: 'Discuss SEO Goals',
    featured: false,
  },
];
---

<Base
  title="Pricing — Websites Starting at $1,000 | KAIRA"
  description="Transparent pricing: professional websites starting at $1,000, monthly care plans from $75/mo, and custom local SEO engagements."
>
  <section class="mesh-bg">
    <div class="mx-auto max-w-6xl px-5 pb-16 pt-16 text-center md:pt-24">
      <h1 class="reveal text-4xl font-bold tracking-tight text-ink md:text-5xl">
        Simple, honest pricing
      </h1>
      <p class="reveal reveal-delay-1 mx-auto mt-4 max-w-xl text-lg text-ink-soft">
        Websites starting at $1,000. Every project is quoted individually based on what your
        business actually needs.
      </p>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-5 py-16 md:py-20">
    <div class="grid gap-6 lg:grid-cols-3">
      {
        tiers.map((t, i) => (
          <article
            class={`reveal reveal-delay-${i} glass relative flex flex-col rounded-2xl p-8 ${
              t.featured ? 'ring-2 ring-purple shadow-(--shadow-glass-lg)' : ''
            }`}
          >
            {t.featured && (
              <span class="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-purple px-4 py-1 text-xs font-semibold tracking-wide text-[#05070c]">
                MOST POPULAR
              </span>
            )}
            <h2 class="text-lg font-semibold text-ink">{t.name}</h2>
            <p class="mt-3 text-3xl font-bold text-ink">{t.price}</p>
            <p class="text-sm text-ink-muted">{t.cadence}</p>
            <p class="mt-4 text-sm leading-relaxed text-ink-soft">{t.blurb}</p>
            <ul class="mt-6 flex-1 space-y-2.5">
              {t.features.map((f) => (
                <li class="flex items-start gap-2.5 text-sm text-ink-soft">
                  <svg class="mt-0.5 shrink-0 text-purple" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M20 6L9 17l-5-5" />
                  </svg>
                  {f}
                </li>
              ))}
            </ul>
            <a
              href="/contact"
              class={`mt-8 rounded-full px-6 py-3 text-center text-sm font-semibold transition-transform duration-150 ease-(--ease-press) hover:scale-[1.02] active:scale-[0.98] ${
                t.featured
                  ? 'bg-purple text-[#05070c] shadow-(--shadow-cta)'
                  : 'border border-purple/25 bg-white/[0.04] text-purple'
              }`}
            >
              {t.cta}
            </a>
          </article>
        ))
      }
    </div>

    <div class="reveal mx-auto mt-14 max-w-2xl rounded-2xl bg-purple-ghost p-7 text-center">
      <h2 class="font-semibold text-ink">How payment works</h2>
      <p class="mt-2 text-sm leading-relaxed text-ink-muted">
        Website projects are quoted up front — typically a deposit to begin and the balance at
        launch, invoiced securely through Stripe. No surprises, no hidden fees.
      </p>
    </div>
  </section>
</Base>
